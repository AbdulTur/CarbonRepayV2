{
  "name": "carbonrepay",
  "version": "1.0.0",
  "scripts": {
      "start": "cd backend && npm install && node server.js",
      "heroku-postbuild": "cd frontend && npm install && npm run build && cd .. && cp -r frontend/build backend/build"
  },
  "engines": {
      "node": "14.x"
  },
  "dependencies": {
      "dotenv": "^8.2.0"
  }
}
